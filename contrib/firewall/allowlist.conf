# Ozwell Studio — outbound allowlist
# Domains and CIDRs the workspace container may reach.
# One entry per line. Lines starting with # are comments.
# CIDR notation supported (e.g., 10.0.0.0/8).
#
# Additional hosts can be added at runtime via the
# OZWELL_ALLOW_HOSTS env var (comma-separated domains/CIDRs).

# ── AI Model Providers ──
api.anthropic.com
# api.openai.com
# generativelanguage.googleapis.com

# ── Git Hosting ──
github.com
ssh.github.com
objects.githubusercontent.com
raw.githubusercontent.com
# gitlab.com

# ── Package Registries: NPM ──
registry.npmjs.org

# ── Package Registries: PyPI / pip ──
pypi.org
files.pythonhosted.org

# ── Package Registries: APT (Debian) ──
deb.debian.org
security.debian.org

# ── Container / Extension Registries ──
ghcr.io
open-vsx.org
